var Data = [];
function Mapping(){
  function loadData(){
    var ca = document.cookie.split(';');
    Dir = data[];
    for(var i=0; i<ca.length; i++) {
	  If(ca[i].split('/').length != 1){
      if(){
        
      }
	  }
	}
    return "";
	
  }
}